#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;

	do {
		if (n < 10)
			putchar('0');
		putchar(n + '0');
		if (n < 89)
		{
			putchar(',');
			putchar(' ');
		}
		n++;
	} while (n < 90);
	putchar('\n');
	return (0);
}
